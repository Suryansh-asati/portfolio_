/*
 Legacy Node server for the portfolio site.
 NOTE: This repo now uses `api/index.ts` (Vercel Serverless Function) as the single entry point
 for both pages and APIs. This file is kept for reference but is not used by vercel.json.
*/

// Deprecated legacy server file. The app uses `api/index.ts` now.
// Kept as an empty stub to avoid confusion.
const express = require('express');
